Pattern content currently lives in `lib/patterns.ts` for type-safe MVP iteration.

This folder is reserved for a future MDX migration, where each pattern can become
an editable article while keeping the same `Pattern` data contract.
